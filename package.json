{
  "name": "tabs2notion",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "find extension backend scripts -name '*.js' -print0 | xargs -0 -n1 node --check",
    "validate:release": "node scripts/validate-release.mjs",
    "package": "npm test && npm run check && npm run validate:release && bash scripts/package-release.sh"
  }
}
