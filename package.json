{
  "name": "tabs2notion",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "find extension -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
